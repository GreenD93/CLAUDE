# 글로벌 작업 선호도

## 사용자 프로그래밍 스타일

> 이 섹션은 사용자의 업무 지시 패턴과 의사결정 스타일을 관찰하여 정리한 것이다.
> 다음 프로젝트에서도 이 스타일에 맞춰 작업한다.

### 의사결정
- **구조적 해결을 선호한다.** 증상 패치(하드코딩 분기, 플래그)를 제시하면 바로 교정한다.
- **"왜?"를 먼저 묻는다.** 기능을 넣기 전에 "이게 지금 필요한 증거가 있나?"를 확인한다.
- **실행 후 판단을 바꿀 수 있다.** 초기 결정도 실제로 돌려보고 문제가 드러나면 전환한다. (예: Memory 분리 → 통합)
- **LLM과 코드의 경계를 명확히 구분한다.** 룰 기반으로 LLM 역할을 대체하려 하면 견제한다.

### 코드 품질
- **네이밍에 집요하다.** 역할이 안 드러나는 이름(chat_agent, logic.py)을 바로 지적한다.
- **파일 구조가 flat하면 안 된다.** 한 파일에 여러 역할이 몰리거나, 디렉터리 없이 나열되면 재구성을 요청한다.
- **인터페이스가 눈에 드러나야 한다.** 서비스별 UI 계약, API 엔드포인트 등이 코드 구조에서 바로 보여야 한다.
- **더미 데이터를 허용하지 않는다.** 실제 데이터를 조사해서 채워야 한다.

### 작업 흐름
- **계획 → 확인 → 실행.** 큰 변경은 계획을 먼저 보여주고 확인받은 뒤 진행한다.
- **한 세션에 너무 많이 쌓이면 끊는다.** 커밋하고 다음 세션으로 넘긴다.
- **문서 동기화를 빼먹으면 지적한다.** 코드 변경 후 md 파일 업데이트를 항상 요구한다.
- **"다음 세션에서"라고 하면 진짜 다음 세션이 온 것이다.** 계획을 기록하고 바로 실행한다.

## 커뮤니케이션
- 한국어로 대화한다
- 불필요한 설명 없이 간결하게
- 코드 변경 전 의도가 불명확하면 먼저 확인

## 코딩 원칙
- 오버엔지니어링 금지: 현재 필요한 것만 구현. **아직 돌려보지 않은 최적화는 하지 않는다.**
  - "나중에 필요할 수도 있으니" → 지금 필요한 증거가 있을 때만
  - 파이프라인이 이미 있으면 수집 방식만 교체하면 되니, 구조를 미리 복잡하게 만들지 않는다
- 새 파일 생성보다 기존 파일 편집 우선
- 주석은 로직이 자명하지 않을 때만
- 사용하지 않는 코드·주석 남기지 않기
- **더미 데이터로 끝내지 않는다**: 샘플이 필요하면 실제 데이터를 조사해서 채운다

## 설계 철학

> "동작하는 코드를 만든 다음, 다음 사람이 읽고 확장할 수 있을 때까지 구조를 다듬는다."
> 기능 개발은 빠르게, 그 후 구조·네이밍·문서를 계속 정제하는 반복적 정련(iterative refinement).

- **결정론적 코드 우선**: 코드로 확실히 제어할 수 있으면 LLM에 맡기지 않는다. LLM 출력은 신뢰하지 않고 반드시 코드로 검증한다. 금전·상태 변경은 코드 검증 필수.
- **단일 소유권 (SSOT)**: 하나의 정보는 한 곳에서만 정의. 중복은 불일치의 씨앗이다.
- **역할 분리**: 각 클래스가 자기 역할만 하고, 책임 경계를 코드 구조로 강제한다. 규칙 문서에만 의존하지 않는다.
- **선언으로 조립**: 설정 파일(yaml/json)로 선언하면 프레임워크가 자동 조립. 개발자는 선언만, 배선은 코드가.
  - 선언 파일은 스키마와 작성 규칙을 문서화한다. Core가 조립에 사용하는 파일이므로 모든 프로젝트에 영향.
  - **필드 생략 ≠ 빈 값**: 생략은 "모름/미정", 빈 값(`[]`, `{}`)은 "의도적으로 사용 안 함". 사용하지 않는 필드도 빈 값으로 명시한다.
- **모든 루프에 상한선**: 무한루프·무한대기 없음. 상한 초과 시 자연스럽게 복구하는 탈출구를 항상 마련한다.
- **Core에 비즈니스 로직 금지**: Core는 인터페이스와 공통 패턴만. 비즈니스 로직은 프로젝트 계층에서.
- **레이어 간 경계 엄수**: 레이어 간 통신은 공개 인터페이스(이벤트, 주입 파라미터)로만. 내부 저장소를 직접 참조하지 않는다.
- **UX에서 구조를 도출**: "사용자가 어떤 경험을 해야 하나?"에서 출발하여 데이터·이벤트 설계를 역추적한다.
- **정책값은 설정 가능하게**: 매직넘버(턴 수 제한, 요약 임계값 등)는 .env/config로 분리. 코드 수정 없이 정책 변경 가능하게.

## 구조·가독성 원칙
- **네이밍 = 역할 설명**: 파일명·클래스명·설정 경로 모두 이름만 보고 역할을 알 수 있어야 한다
  - 범용 이름 금지: 역할이 드러나는 이름을 사용
- **파일 하이어러키 준수**: 한 파일에 역할이 다른 클래스가 3개 이상이면 분리한다
  - flat 구조 금지: 관련 파일은 하위 디렉터리로 그룹핑
- **설정 파일의 경로·값은 처음 보는 사람이 바로 이해할 수 있게** 주석이나 규칙을 명시
- **하드코딩보다 설정 가능한 구조**: 매직넘버·정책값은 설정 파일로 분리, 프로젝트별 커스텀 가능하게
- **전체 구조가 한눈에 읽혀야 한다**: 개별 코드보다 아키텍처 전체 그림이 먼저 보이게
- **문서 ↔ 코드 동기화**: 코드 변경 시 관련 문서 즉시 반영
- **예외 상황에서 자연스러운 복구**: 에러·엣지케이스에서 사용자 경험이 끊기지 않게
- **인터페이스가 코드 구조에 드러나야 한다**: 서비스별 UI 계약, API 엔드포인트 등이 폴더/파일 구조에서 바로 보여야 확장성이 생긴다

## 문서 작성 스타일

### 구조
- **실제 입력 예시로 시작**: 추상 설명 전에 구체적 시나리오부터
- **큰 그림 → 세부 → 관계도 순서**: 전체 흐름 먼저, 각 클래스 상세, 마지막에 클래스 간 관계
- **각 클래스는 3단 구조**: 역할 / 소유(의존성) / 핵심 메서드
- **역방향 매핑 테이블**: "뭘 바꾸려면 어디를 보나" — 목적 → 수정 파일
- **대비 테이블**: 코드 vs LLM, 실수 vs 올바른 방법 등 양쪽을 나란히
- 프로젝트의 실행 흐름·클래스 관계·데이터 흐름은 ASCII 다이어그램으로 문서화
- **차트만 보고 구조를 파악할 수 있어야 한다** — 본문 없이도 이해 가능한 수준
- 박스 `┌─ ClassName ─┐`, 화살표 `→ ▼ ──→`, 트리 `├─ └─`, 점선 `·····`
- 인라인 주석 `← 설명`, 차트 끝에 "핵심: ..." 한 줄 요약
- 프로세스 순서는 번호 `① ② ③`으로 표시

### CLAUDE.md ↔ STRUCTURE.md 분리 규칙
- **CLAUDE.md**: 규칙·원칙·테이블·간결한 요약만. ASCII 차트를 직접 넣지 않는다.
- **STRUCTURE.md**: ASCII 구조도 전용. 클래스 관계·실행 흐름·데이터 흐름·수정 가이드 차트.
- CLAUDE.md에는 구조도 요약(10줄 이내) + `> **시스템 구조도**: [STRUCTURE.md](STRUCTURE.md)` 링크.
- **동기화 규칙**: 코드 리팩토링으로 클래스명·관계·흐름이 바뀌면 STRUCTURE.md도 함께 업데이트.
- **이유**: CLAUDE.md는 세션 시작 시 자동 로드되므로 40k 이하로 유지. 구조도는 필요할 때만 참조.

## 수정의 깊이 — 3단계를 항상 완주한다

> "동작하게 만드는 건 1단계일 뿐. 구조를 고치고, 문서로 남겨야 끝이다."

```
  ① 원인 분석    왜 이 버그가 구조적으로 가능했는가?
                  증상(플래그·분기 추가)이 아닌 근본 원인을 찾는다.
  ② 적절한 계층에서 수정
                  개별 프로젝트 문제 → 프로젝트 코드에서 수정
                  여러 프로젝트에서 반복될 패턴 → Core로 승격해서 Template화
                  임시 해결이 불가피하면 TODO + 구조적 해결 방향 명시
  ③ 문서화 + 재발 방지
                  프로젝트 CLAUDE.md에 패턴 기록 (흔한 실수, 구조적 결함 방지)
                  CHANGELOG 업데이트
                  새 분기 추가 시 테스트도 추가
```

**하지 않는 것:**
- 증상만 고치고 끝내기 (하드코딩 플래그, 특정 케이스 분기 추가)
- 프로젝트에서 반복되는 패턴을 매번 프로젝트마다 개별 구현
- 고쳤으면 됐지 하고 문서·테스트 생략

## 버그 수정 시 동일 패턴 전수 점검

> "한 곳에서 발생한 버그는 같은 패턴을 쓰는 모든 곳에서 발생할 수 있다."

버그를 발견하면 해당 파일만 고치지 않는다. 반드시 아래 절차를 따른다:

```
  ① 발생 지점 수정
  ② grep으로 동일 패턴을 사용하는 모든 파일을 전수 조사
  ③ 같은 문제가 있는 곳을 모두 수정
  ④ 근본 원인이 "규칙으로만 강제되고 코드로 강제되지 않은 것"이면
     코드 구조를 바꿔서 잘못된 사용이 불가능하게 만든다
```

**전수 점검 대상 판단 기준:**
- 같은 메서드/함수를 호출하는 다른 클래스
- 같은 인터페이스를 구현하는 다른 구현체
- 같은 패턴(상속, 래퍼, 콜백)을 따르는 다른 모듈

## 래퍼·어댑터 추가 시 전수 마이그레이션

> "래퍼를 만들었으면, 이전 경로의 모든 호출자를 래퍼로 마이그레이션한다."

기존 메서드 A를 감싸는 래퍼 B를 만들 때:

```
  ① grep으로 A의 모든 호출처를 전수 조사한다
  ② 호출처를 전부 B로 교체한다
  ③ A를 직접 호출하는 새 코드가 나오지 않도록 B의 docstring에 "유일한 진입점" 명시
  ④ "하위호환을 위해 이전 경로도 남겨둠" → 하지 않는다. 이전 경로가 남으면 반드시 누군가 쓴다.
```

## 프레임워크·라이브러리 위에서 작업할 때

> "프레임워크의 제약을 먼저 파악하고, 가능한 API를 확인한 뒤 계획을 세운다."

```
  ① 프레임워크가 지원하는 API로 목표를 달성할 수 있는지 먼저 확인
  ② API로 불가능하면, CSS/JS 패치가 아닌 구조적 대안을 검토
  ③ 패치를 하나 넣었는데 다른 곳이 깨지면, 패치 위에 패치를 쌓지 않고 접근을 재검토
  ④ "안 되면 다른 방법" — 같은 방향의 패치를 3번 이상 반복하면 방향 자체가 틀린 것
```

## 작업 방식
- 큰 변경은 계획 먼저 제시 후 진행. **구조 변경 시 ASCII 차트로 Before/After를 보여주고 확인받는다.**
- 기능 추가 후 반드시 기존 테스트 통과 확인. 실패하면 수정 후 넘어간다.
- 버그 수정 이력은 프로젝트 MEMORY.md에 기록
- 새 프로젝트·아키텍처 변경은 프로젝트 CLAUDE.md에 반영
- **같은 실수가 2번 이상 발생하면 프로젝트 CLAUDE.md "흔한 실수" 테이블에 추가한다.** 사용자가 교정한 패턴은 재발 방지를 위해 반드시 문서화.
- **사용자가 교정한 설계 판단은 즉시 반영한다.** 제안을 견제당하면 원칙에 비추어 재검토하고, 원칙이 맞으면 따른다.
- **세션이 길어지면 커밋 후 다음 세션으로 넘긴다.** MEMORY.md에 TODO를 기록하여 이어받을 수 있게.

## 문서 계층 — 어디에 뭘 쓰는가

```
~/.claude/CLAUDE.md                  ← [이 파일] 글로벌. 모든 프로젝트에 적용.
                                       코딩 원칙, 설계 철학, 사용자 스타일.
                                       프로젝트 특정 사례·코드를 넣지 않는다.

프로젝트/CLAUDE.md                   ← 프로젝트별. git에 포함 (팀 공유).
                                       아키텍처 규칙, 원칙, 절대 규칙.
                                       구조도는 요약 + STRUCTURE.md 링크만.

프로젝트/STRUCTURE.md                ← ASCII 구조도 전용. git에 포함.
                                       클래스 관계·실행 흐름·데이터 흐름 차트.
                                       CLAUDE.md에서 링크로 참조.

~/.claude/projects/{경로}/           ← 프로젝트별. 로컬만 (본인만).
  memory/MEMORY.md                     실무 지식, 아키텍처 결정 이력, 버그 패턴.
                                       다음 세션 TODO도 여기에 기록.
```

## 주요 워크스페이스
- AI 오케스트레이터: ~/workspace/llm-orchestrator
